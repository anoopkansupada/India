// Configuration file for API keys and sensitive data
//
// SETUP INSTRUCTIONS:
// 1. Copy this file and rename it to: config.js
// 2. Replace 'YOUR_API_KEY_HERE' with your actual Google Maps API key
// 3. Add config.js to .gitignore so it's never committed to Git
// 4. Keep this example file (config.example.js) in Git as a template

const CONFIG = {
    GOOGLE_MAPS_API_KEY: 'YOUR_API_KEY_HERE'
};
